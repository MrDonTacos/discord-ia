module.exports = {
    name: 'add',
    description: 'Añade un elemento a la lista especificada',
    usage: '<Titulo> <Descripcion?> <Duración?> <NombreLista>', 
    args: true,
	execute(message, args) { 
		if (args.length != 1) {
            
		}
        
	},
};